// TODO: presentation component for an indiv post
// TODO: data passed in props

import React from 'react';

const Post = (props) => (
  <>
    <div>here's a {props.postType} post</div>
    <div>here's a {props.postType} post</div>
    <div>here's a {props.postType} post</div>
  </>
)

export default Post;
